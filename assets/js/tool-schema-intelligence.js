(()=>{const U=window.ReportUI,analyse=document.querySelector('#schema-analyse-form'),generate=document.querySelector('#schema-generate-form'),analysisOut=document.querySelector('#schema-analysis-result'),generatorOut=document.querySelector('#schema-generator-result');const renderCode=data=>{const code=JSON.stringify(data.generated,null,2);return `<section class="result-shell"><div class="result-head"><div>${U.tag(data.generationSource||data.sourceLabel||'Generated','ai')}<h2 style="font-size:2rem;margin-top:12px">${U.esc(data.chosenType||'Schema')} JSON-LD</h2><p>Generated from supplied facts and visible page evidence. Review empty fields before implementation.</p></div></div><div class="result-body"><pre class="code-block" data-schema-code>${U.esc(code)}</pre><div class="actions"><button class="btn btn-primary" data-copy-schema>Copy JSON-LD</button><button class="btn btn-light" data-download-schema>Download JSON</button><a class="btn btn-light" target="_blank" rel="noopener" href="https://validator.schema.org/">Schema.org Validator</a><a class="btn btn-light" target="_blank" rel="noopener" href="https://search.google.com/test/rich-results">Google Rich Results Test</a></div></div></section>`};const bindCode=(root,data)=>{root.querySelector('[data-copy-schema]')?.addEventListener('click',async()=>{await navigator.clipboard.writeText(JSON.stringify(data.generated,null,2));alert('Schema copied.')});root.querySelector('[data-download-schema]')?.addEventListener('click',()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data.generated,null,2)],{type:'application/json'}));a.download='pajee-seo-schema.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)})};const pager=(mount,rows,render,size=25)=>{let page=1;const draw=()=>{const total=Math.max(1,Math.ceil(rows.length/size)),slice=rows.slice((page-1)*size,page*size);mount.innerHTML=render(slice)+`<div class="v10-pagination"><span class="state">Page ${page} of ${total} · ${rows.length} URLs</span><div class="controls"><button data-prev ${page<=1?'disabled':''}>Previous</button><button disabled>${page}</button><button data-next ${page>=total?'disabled':''}>Next</button></div></div>`;mount.querySelector('[data-prev]')?.addEventListener('click',()=>{page--;draw()});mount.querySelector('[data-next]')?.addEventListener('click',()=>{page++;draw()})};draw()};analyse?.addEventListener('submit',async e=>{e.preventDefault();const payload=Object.fromEntries(new FormData(analyse));analysisOut.innerHTML=U.loading('Analysing structured data…',['Discovering the requested page scope','Detecting JSON-LD, Microdata and RDFa','Checking syntax and common fields','Matching page intent to relevant schema']);try{const data=await U.api('schema-intelligence',{method:'POST',body:{...payload,maxPages:Number(payload.maxPages||200)}}),summary=data.summary||{},recMap=new Map((data.pageRecommendations||[]).map(x=>[x.url,x])),pie=PajeeCharts.donut([{label:'Pages with schema',value:summary.pagesWithSchema||0},{label:'Pages without schema',value:summary.pagesWithoutSchema||0},{label:'Invalid blocks',value:summary.invalidBlocks||0}],{centerLabel:'Schema health'});analysisOut.innerHTML=`<section class="result-shell"><div class="result-head"><div>${U.tag(data.sourceLabel,'live')}<h2 style="font-size:2rem;margin-top:12px">Schema health and page recommendations</h2><p>${U.esc(data.siteIntent||'Website intent analysed')} · ${U.esc(data.analysisSource||'')}</p></div><button class="btn btn-primary" data-generate-correct>Generate primary recommendation</button></div><div class="result-body"><div class="metric-grid">${U.metric('Pages checked',summary.pagesChecked,'good')}${U.metric('With schema',summary.pagesWithSchema,summary.pagesWithSchema?'good':'warn')}${U.metric('Without schema',summary.pagesWithoutSchema,summary.pagesWithoutSchema?'warn':'good')}${U.metric('Invalid blocks',summary.invalidBlocks,summary.invalidBlocks?'poor':'good')}</div><div class="v10-chart-grid"><article class="v10-chart-card"><h3>Schema distribution</h3>${pie}</article><article class="v10-chart-card"><h3>Priority recommendations</h3><div class="v10-compact-list">${(data.recommendations||[]).map(r=>`<article class="v10-compact-finding"><i></i><div><strong>${U.esc(r.type)}</strong><small>${U.esc(r.reason)}</small></div></article>`).join('')}</div></article></div><section class="report-section" style="margin-top:24px"><div class="report-section-head"><div><span class="eyebrow">Page-wise evidence</span><h3>Detected schema, status and recommended type</h3></div></div><div data-schema-pages></div></section><div class="notice notice-blue" style="margin-top:18px">${U.esc(data.validatorNotice||'')}</div></div></section>`;const mount=analysisOut.querySelector('[data-schema-pages]'),rows=data.pages||[];pager(mount,rows,slice=>U.pageTable(slice,[{label:'Page URL',render:r=>`<a href="${U.esc(r.url)}" target="_blank" rel="noopener">${U.esc(r.url)}</a>`},{label:'Detected types',render:r=>(r.types||[]).map(U.esc).join(', ')||'None'},{label:'Status',render:r=>r.invalidBlocks?'Invalid JSON-LD':r.types?.length?'Schema found':'Missing schema'},{label:'Suggested type',render:r=>U.esc(recMap.get(r.url)?.recommendedTypes?.[0]?.type||data.recommendations?.[0]?.type||'WebPage')},{label:'Action',render:r=>`<button class="btn btn-light btn-sm" data-page-generate="${U.esc(r.url)}" data-page-type="${U.esc(recMap.get(r.url)?.recommendedTypes?.[0]?.type||data.recommendations?.[0]?.type||'WebPage')}">Generate</button>`}]));analysisOut.addEventListener('click',async ev=>{const b=ev.target.closest('[data-page-generate]');if(!b)return;b.disabled=true;b.textContent='Generating…';try{const generated=await U.api('schema-intelligence',{method:'POST',body:{url:b.dataset.pageGenerate,scope:'page',generate:true,type:b.dataset.pageType}});analysisOut.insertAdjacentHTML('beforeend',renderCode(generated));bindCode(analysisOut,generated);analysisOut.lastElementChild.scrollIntoView({behavior:'smooth',block:'start'})}catch(err){alert(err.message)}finally{b.disabled=false;b.textContent='Generate'}});analysisOut.querySelector('[data-generate-correct]')?.addEventListener('click',async()=>{const b=analysisOut.querySelector('[data-generate-correct]');b.disabled=true;b.textContent='Generating…';try{const generated=await U.api('schema-intelligence',{method:'POST',body:{...payload,maxPages:Number(payload.maxPages||200),generate:true,type:data.chosenType}});analysisOut.insertAdjacentHTML('beforeend',renderCode(generated));bindCode(analysisOut,generated)}catch(err){alert(err.message)}finally{b.disabled=false;b.textContent='Generate primary recommendation'}})}catch(err){U.statusMessage(analysisOut,err.message,'red')}});generate?.addEventListener('submit',async e=>{e.preventDefault();const raw=Object.fromEntries(new FormData(generate)),fields={...raw};delete fields.url;delete fields.type;generatorOut.innerHTML=U.loading('Generating schema…',['Validating inputs','Building a Schema.org object','Normalising JSON-LD']);try{const data=await U.api('schema-intelligence',{method:'POST',body:{mode:'generate-manual',url:raw.url,type:raw.type,fields}});generatorOut.innerHTML=renderCode(data);bindCode(generatorOut,data)}catch(err){U.statusMessage(generatorOut,err.message,'red')}})})();