(()=>{
  const U=window.ReportUI;
  const analyse=document.querySelector('#schema-analyse-form');
  const generate=document.querySelector('#schema-generate-form');
  const analysisOut=document.querySelector('#schema-analysis-result');
  const generatorOut=document.querySelector('#schema-generator-result');
  const renderCode=(data)=>{const code=JSON.stringify(data.generated,null,2);return `<section class="result-shell"><div class="result-head"><div class="section-head"><div class="copy">${U.tag(data.generationSource||data.sourceLabel||'Generated','ai')}<h2 style="font-size:2rem;margin-top:12px">${U.esc(data.chosenType||'Schema')} JSON-LD</h2><p>Generated from supplied facts and visible page evidence. Review empty fields before implementation.</p></div></div></div><div class="result-body"><pre class="code-block" data-schema-code>${U.esc(code)}</pre><div class="actions"><button class="btn btn-primary" data-copy-schema>Copy JSON-LD</button><button class="btn btn-light" data-download-schema>Download JSON</button><a class="btn btn-light" target="_blank" rel="noopener" href="https://validator.schema.org/">Schema.org Validator</a><a class="btn btn-light" target="_blank" rel="noopener" href="https://search.google.com/test/rich-results">Google Rich Results Test</a></div></div></section>`};
  const bindCode=(root,data)=>{root.querySelector('[data-copy-schema]')?.addEventListener('click',async()=>{await navigator.clipboard.writeText(JSON.stringify(data.generated,null,2));alert('Schema copied.')});root.querySelector('[data-download-schema]')?.addEventListener('click',()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data.generated,null,2)],{type:'application/json'}));a.download='pajee-seo-schema.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)})};
  analyse?.addEventListener('submit',async e=>{
    e.preventDefault();const payload=Object.fromEntries(new FormData(analyse));analysisOut.innerHTML=U.loading('Analysing structured data…',['Crawling selected pages','Detecting JSON-LD, Microdata and RDFa','Checking syntax and common fields','Understanding page intent']);
    try{const data=await U.api('schema-intelligence',{method:'POST',body:{...payload,maxPages:Number(payload.maxPages||15)}});const summary=data.summary||{};analysisOut.innerHTML=`<section class="result-shell"><div class="result-head"><div class="section-head"><div class="copy">${U.tag(data.sourceLabel,'live')}<h2 style="font-size:2rem;margin-top:12px">Schema health summary</h2><p>${U.esc(data.siteIntent||'Website intent analysed')} · ${U.esc(data.analysisSource||'')}</p></div><button class="btn btn-primary" data-generate-correct>Generate recommended schema</button></div></div><div class="result-body"><div class="metric-grid">${U.metric('Pages checked',summary.pagesChecked,'good')}${U.metric('Pages with schema',summary.pagesWithSchema,summary.pagesWithSchema?'good':'warn')}${U.metric('Pages without schema',summary.pagesWithoutSchema,summary.pagesWithoutSchema?'warn':'good')}${U.metric('Invalid blocks',summary.invalidBlocks,summary.invalidBlocks?'poor':'good')}</div><div class="grid-2" style="margin-top:24px"><article class="card"><h3>Detected types</h3><div class="hero-proof" style="margin-top:14px">${(summary.types||[]).map(t=>`<span>${U.esc(t)}</span>`).join('')||'<p>No structured-data types detected.</p>'}</div></article><article class="card"><h3>Recommended types</h3><div class="issue-list" style="margin-top:14px">${(data.recommendations||[]).map(r=>U.issue({status:'info',title:r.type,detail:r.reason})).join('')}</div></article></div><div class="report-section" style="margin-top:24px"><div class="report-section-head"><div><span class="eyebrow">Page evidence</span><h3>Findings by URL</h3></div></div><div class="issue-list">${(data.pages||[]).flatMap(p=>(p.findings||[]).map(f=>U.issue({...f,status:f.status==='fail'?'fail':f.status==='warn'?'warn':'pass',url:p.url}))).join('')||'<p>No issues returned.</p>'}</div></div><div class="notice notice-blue" style="margin-top:18px">${U.esc(data.validatorNotice||'')}</div></div></section>`;
      analysisOut.querySelector('[data-generate-correct]')?.addEventListener('click',async()=>{const btn=analysisOut.querySelector('[data-generate-correct]');btn.disabled=true;btn.textContent='Generating…';try{const generated=await U.api('schema-intelligence',{method:'POST',body:{...payload,maxPages:Number(payload.maxPages||15),generate:true,type:data.chosenType}});analysisOut.insertAdjacentHTML('beforeend',renderCode(generated));bindCode(analysisOut,generated)}catch(err){alert(err.message)}finally{btn.disabled=false;btn.textContent='Generate recommended schema'}});
    }catch(err){U.statusMessage(analysisOut,err.message,'red')}
  });
  generate?.addEventListener('submit',async e=>{
    e.preventDefault();const raw=Object.fromEntries(new FormData(generate));const fields={...raw};delete fields.url;delete fields.type;generatorOut.innerHTML=U.loading('Generating schema…',['Validating user inputs','Building Schema.org object','Normalising JSON-LD']);
    try{const data=await U.api('schema-intelligence',{method:'POST',body:{mode:'generate-manual',url:raw.url,type:raw.type,fields}});generatorOut.innerHTML=renderCode(data);bindCode(generatorOut,data)}catch(err){U.statusMessage(generatorOut,err.message,'red')}
  });
})();
