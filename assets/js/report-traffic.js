(async()=>{const U=ReportUI,p=U.params(),root=document.querySelector('#traffic-report');root.innerHTML=U.loading('Estimating public traffic signals…',['Crawling the website','Understanding business maturity','Estimating broad channel bands','Building recommendations']);try{const d=await U.api('traffic-estimate',{params:p});const logo=document.querySelector('[data-site-logo]');logo.innerHTML=d.site?.favicon?`<img src="${U.esc(d.site.favicon)}" alt="">`:U.esc((d.site?.name||'P').slice(0,1));document.querySelector('[data-report-subtitle]').textContent=`${d.site?.name||''} · ${d.dataType}`;root.innerHTML=`<section class="report-section"><div class="report-section-head"><div>${U.tag('AI Estimated','ai')}<h2>Public traffic and behaviour profile</h2><p>${U.esc(d.disclaimer)}</p></div></div><div class="metric-grid">${U.metric('Monthly range',d.monthlyRange,'warn')}${U.metric('Confidence',d.confidence,'warn')}${U.metric('Traffic maturity',d.trafficMaturity,'warn')}${U.metric('Engagement signal',d.engagementBands?.engagementRate||'Unknown','warn')}</div></section><section class="report-section"><div class="report-section-head"><div><span class="eyebrow">Channel mix</span><h2>Broad public estimate</h2></div></div><div class="grid-3">${(d.channelMix||[]).map(x=>`<article class="card"><h3>${U.esc(x.channel)}</h3><p style="font-size:1.7rem;color:var(--ink);font-weight:900">${U.esc(x.range)}</p></article>`).join('')}</div></section><section class="report-section"><div class="grid-2"><div><h3>Public signals</h3><div class="issue-list" style="margin-top:14px">${(d.signals||[]).map(U.issue).join('')}</div></div><div><h3>Recommended next steps</h3><div class="issue-list" style="margin-top:14px">${(d.recommendations||[]).map(U.issue).join('')}</div></div></section><section class="cta-band"><div><h2>Replace estimates with verified GA4 data.</h2><p>Connect read-only Analytics access to review users, sessions, channels, landing pages and conversions.</p></div><a class="btn btn-light" href="/tools/traffic-analytics/">Open verified analytics</a></section>`}catch(e){U.statusMessage(root,e.message,'red')}})();
